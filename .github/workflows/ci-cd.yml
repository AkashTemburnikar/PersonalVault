name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-dockerize:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore src/PersonalVault.sln

      - name: Build
        run: dotnet build PersonalVault.sln --configuration Release --no-restore

      - name: Build Docker image
        run: docker build -t personalvaultapi:latest -f Dockerfile .

    # Optional: push to Docker Hub
    # - name: Login to Docker Hub
    #   uses: docker/login-action@v2
    #   with:
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}

    # - name: Push image to Docker Hub
    #   run: docker tag personalvaultapi your-docker-username/personalvaultapi:latest && docker push your-docker-username/personalvaultapi:latest